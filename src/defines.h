#ifndef DEFINES
#define DEFINES

#define CTRL_KEY(key) ((key) & 0x1f)

#define KILO_VERSION "0.0.1"

enum editorKey {
	ARROW_LEFT = 1000,
	ARROW_DOWN,
	ARROW_UP,
	ARROW_RIGHT,
	PAGE_UP,
	PAGE_DOWN
};

#endif // !DEFINES
